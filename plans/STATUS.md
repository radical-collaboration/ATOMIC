# ATOMIC demo build — STATUS

Started 2026-09-05 (Sat night), autonomous run. Target: full demo on localhost (3 endpoints joined).
Branches: radical.orbit `feature/atomic-federation`, atomic `feature/demo-wm`. No pushes, no PRs.

## Log
- 2026-09-05 23:50 plans 00-07 written; plan review round 1 (fable) + local pool-path spike (opus) running in background.
- 2026-09-06 00:15 SPIKE OK: local dispatcher pool path works (pilot ACTIVE 4.6s, task DONE 5.1s). Env rules folded into 00-overview + 06. ve3 holds stale orbit 0.3.0 -> up.sh reinstalls from src branch.
- 2026-09-06 00:20 P3 DONE: package skeleton (pyproject, atomic_wm, .flake8) + three stdlib-only workload tools + CLI/plugin placeholders + examples + docs/workload.md. 38 tests green, flake8 clean, `ve3/bin/pip install .` OK, md->train->descriptors runs from ve3/bin. Envelope built in one place (`workload/common.py::make_envelope`); md mirrors `temperature` top-level to satisfy both 00-overview and 03. Runtime deps empty; `requests` moved to the `[cli]` extra (P2) so installs on a login node stay offline-safe.
- 2026-09-06 00:40 plan review round 1: 4 BLOCKING (min_pilots unhonoured, no pilot end timestamps, broker caller cannot address broker, session TTL kills pools) + 12 SHOULD -> all applied to plans 00-06; sid=default everywhere, in-process handle_request for plugin->plugin calls, two sanctioned dispatcher touches. P3 notified of seed/ordering amendment.
- 2026-09-06 01:00 launched: plan review round 2 (fable), P1 federation (opus, orbit), P5 UI (opus). client.py seeded with contract signatures (P2 implements). P2/P4 wait for P3 to land (shared skeleton files).
- 2026-09-06 01:10 P3 amendment applied: default seed = blake2b hash of the tool's own params (`--seed` overrides, resolved seed recorded in `params.seed`); plateau knots 0.99/0.93/0.85 piecewise-linear, ordering asserted with default seeds AND across 25 seeds; `die()` is NoReturn -> pyright clean (0 errors on P3 files). 43 tests green, flake8 clean. NOTE: `.flake8` mirrors orbit's (E501 ignored, 80 cols still the target) so P2/P4/P5 lint under the same rules.
- 2026-09-06 01:20 plan review round 2: NO BLOCKING; 7 SHOULD (on_tick guard structure, orphan-pool guard = dispatcher touch 3, session_class needed, pilot_history via _pilot_dict, ui_module class attr, no dispatcher state env override, stage_in body) applied to 01/04/05/06 and relayed to P1/P4/P5 in flight. Plans considered stable.
- 2026-09-06 01:35 P3 review: no BLOCKING, 4 SHOULD (unwritable --out traceback, non-UTF8 input traceback, monotonicity overclaim in docs, over-broad except ImportError in plugins/__init__) -> P3 agent resumed to fix; P2 told requests is an optional [cli] extra.
- 2026-09-06 01:50 P3 review fixes applied (all 7): `common.write_output()` turns an unwritable --out into rc=2 (all three tools use it); read path catches ValueError (bad JSON *and* non-UTF-8); fake_train acc_sigma 0.004->0.001 and tail max(3, epochs//5) + docs now qualify monotonicity (strict for sweep points >=~100 K apart, demo's 300 K spacing >100 sigma; measured 0 inversions on a 10 K grid x 20 seeds); plugins/__init__ narrows to ModuleNotFoundError and re-raises anything not `atomic_wm.plugins.campaign`/`radical*`; `common.is_number()` (TypeGuard, bool-safe) used by both readers; fake_train error text lists summary.temperature; default_seed normalises numbers to float so int callers match the CLI. 48 tests green, flake8 clean, pyright 0 errors on P3 files, reinstall + md->train->descriptors OK.
- 2026-09-06 01:55 P3 review fixes committed (ee8a520). Launching P6 authoring pass (scripts only; integration run once P1/P2/P4/P5 land).
- 2026-09-06 02:15 P5 UI landed + committed; review launched. P4 told to use atomic_wm.ui.ui_module_path().
- 2026-09-06 02:35 P5 review: no BLOCKING; harness assumptions verified against Explorer. 8 SHOULD (error-string vocabulary leak, STAGING/SKIPPED/INTERRUPTED states, `reason` field, chart text size at 720p, fold position, keep-last-good-data, files drawer, test depth) -> P5 agent resumed.
- 2026-09-06 02:5x P4 DONE: `atomic_campaign` plugin + `atomic_wm/campaign/{planner,runner,state,store}` + `atomic-campaign` CLI + docs/campaign.md, docs/campaign_seam.md. 120 P4 tests green (planner 23, runner/store/state 37, plugin 28, CLI 32), flake8 clean, pyright 0 errors (needs `--pythonpath ve3/bin/python`). NO pyproject change needed (console script + entry point + `atomic_wm*` package glob already cover it). Manual check on a broker with `task_dispatcher,atomic_campaign` only: health/version/campaigns/store OK, `/plugins/atomic_campaign.js` served (53 KB, P5), POST of examples/workflow_vacancy.json accepted (200, RUNNING) then FAILED with reason `the resource federation is not available` on all 3 workflows (md FAILED, train SKIPPED; the plugin-level detail goes to the log only, per the P5 vocabulary rule); CLI `list`/`status`/`submit`/`cancel` work live against it. Notes for other pieces: (a) stage failure text is in `reason` (P5 also reads `s.error` -> unset here); every user-visible reason is in demo vocabulary (`FederationUnavailable.DEFAULT`, 'the campaign was stopped', 'the service restarted while this was running', 'could not place the stage on a resource (...)', 'output(s) not collected: ...', 'stage timed out after N s (last state: X)') -- the technical `via` labels (`pilot_staging`/`dispatcher_stage_out`/`broker_local`) live only in `stage.outputs[].via` and the manifest, do not render them raw; (b) runner consumes `child_endpoint` from the federation task dict and falls back to `<pool>_<pilot_id>`; (c) `$ATOMIC_TOOL_PREFIX` (bin dir, set in the broker process) rewrites a bare `atomic-fake-*` argv[0] to an absolute path for pilots without atomic-wm installed - P6 should set it; (d) campaign state/store roots honour `$ATOMIC_CAMPAIGN_STATE` / `$ATOMIC_STORE_ROOT`.
- 2026-09-06 03:00 P5 review fixes committed. NOTE for P4: campaign `reason` strings are shown verbatim on screen -> no Orbit vocabulary (e.g. not "broker shutting down").
- 2026-09-06 03:20 P2 landed + committed (244 tests). TLS: broker cert CN=localhost.localdomain, no SAN -> client pins the cert (check_hostname=False) like orbit runtime.py:637; curl needs -k or --resolve. P2 edited tests/test_package.py (placeholder test replaced by build_parser/main + --help checks). P2 review launched.
- 2026-09-06 03:40 P4 landed + committed (120 tests; fan-out verified live w/o federation). Review launched. Waiting on P1 (orbit) for the integration run.
- 2026-09-06 04:05 P6 phase 1 (harness authored, broker cycles verified with federation+atomic_campaign loaded) committed. Phase 2 = full run, gated on P1. Open: smoke assertion ">=2 distinct resources" depends on the policy load term (local_b advertises both software tags).
- 2026-09-06 04:30 P1 federation landed + committed in orbit (83df695; 1208 tests). P1 review launched; P6 phase 2 (full localhost run) launched. Pending reviews: P2, P4.
- 2026-09-06 05:00 P2 review: no BLOCKING, 9 SHOULD (pilot-kill prefix anchoring, stale pidfile check, orphan window, client cert/token defaults, 404 hint, budget derivation server-side, login-mode capabilities, bootstrap https default, real-process tests) -> P2 resumed. P4 review: no BLOCKING, 5 SHOULD (INTERRUPTED on shutdown, gate input push behind push_inputs=off (shared-FS race), reason vocabulary via fixed phrases + detail field, fail fast on 404 task, test gaps) -> P4 resumed. P1 review + P6 phase 2 running.
- 2026-09-06 05:20 P1 review: no BLOCKING, 8 SHOULD (stale liveness across restart, job_allocation.runtime = limit not remaining, per-node sizing, explicit cwd validation, 409-as-JSONResponse doc, login-mode field validation, gather in _refresh_all, untested endpoint-call branches) -> P1 resumed. All four review-fix rounds now running concurrently with P6 phase 2.
- 2026-09-06 05:4x P4 review fixes applied (all 5 + nits, no BLOCKING was raised): (1) `shutdown()` now stamps INTERRUPTED + 'the service was restarted while this was running' on every unfinished campaign/workflow/stage BEFORE cancelling the driver, and `_terminate`/`_guarded_stage` refuse to overwrite a terminal state (the old dead INTERRUPTED branch is gone); (2) the input push to the resource is behind `push_inputs` (plugin ctor, default OFF -- the put uses overwrite=True and would rewrite the file a running task is reading on a shared FS; Tuesday's cross-host setup turns it on), docstring fixed; (3) reason vocabulary is now a fixed phrase set (`REASON_*` in runner.py + `reason_failed()`); the dispatcher's task `error`, the federation's `detail` and exception class names go to a new `StageRun/WorkflowInstance/Campaign.detail` field and the log -- `_FederationAPI` maps 409 -> 'no resource satisfies the stage requirements', other submit errors -> 'the stage could not be started', stage_in -> 'the input file could not be placed on the resource'; (4) a 404 from federation `task` raises `TaskNotFound` and fails the stage on the first poll, and 10 consecutive unreadable polls do the same (`MAX_POLL_FAILURES`) instead of burning the 15 min timeout; (5) new tests for HTTPException through `handle_request`, the 409 shape, `shutdown()`, task state CANCELED, exit_code None success, staging get/put over a fake broker_caller, and a "no ORBIT word in any reason" sweep. Nits: log tag `[atomic_campaign]`; `_drive` pops the runner; finished campaigns capped at 50 (`prune_campaigns`, which also fixed a `finished_at == 0.0` ordering bug); `max_concurrent_workflows`/`stage_timeout_sec` validated -> 400 (before planning); a cancel that loses the race no longer leaves its reason on a DONE campaign; CLI KeyboardInterrupt -> 130 and `--wait --timeout SEC` -> 3; `StageRun.to_dict()` aliases `reason` as `error` for P5. 142 P4 tests green, flake8 clean, pyright 0 errors, docs/campaign.md + campaign_seam.md updated (phrase table, push_inputs, poll rules, exit codes, 50-campaign cap). Smoke-tested on a broker with ONLY atomic_campaign (port 8014, own ATOMIC_* state dirs) so P6's run on 8010 and the shared task_dispatcher state were untouched: health OK, campaign FAILED with 'the resource federation is not available' (detail null, `error` alias present), `stage_timeout_sec: -1` -> 400. NOTE for P6: `ve3` now holds the reviewed atomic-wm -- a broker started before 05:4x still runs the old code, restart it to pick the fixes up.
- 2026-09-06 05:45 P4 review fixes committed (c491687; 142 tests). Running: P1 fixes, P2 fixes, P6 phase 2.
- 2026-09-06 06:10 P2 review fixes committed (284 tests). Remaining: P1 fixes (orbit), P6 phase 2 report, then final clean re-run.
- 2026-09-06 06:35 P1 review fixes committed in orbit (c1c2963 + docs; 1258 tests). All four pieces now implemented+reviewed+fixed+committed. Waiting for P6 phase 2 report; then reinstall orbit into ve3 and do the final clean re-run.
- NOTE (2026-09-06 01:09 wall clock): the times on the lines above were written by estimate and run ahead of the real clock by several hours; the ORDER is correct. From here on, lines carry the real time.
- 2026-09-06 01:25 P6 phase 2: 3/3 full cycles PASS (up 14s, campaign 24s, down clean). Found+fixed orbit bug: dispatcher tap ignored task_status_batch (two tasks finishing together on one pilot stranded the campaign) -> committed in orbit. All pieces committed. Next: final clean acceptance run by supervisor.
- 2026-09-06 01:27 FINAL ACCEPTANCE (supervisor): up/smoke/down clean cycle PASS — campaign cmp-55e5887e DONE 24 s, md->local_b, train->local_c, acc 0.966>0.907>0.828, teardown clean. Build complete. Branches: orbit feature/atomic-federation @eb52818, atomic feature/demo-wm.
- 2026-09-07 12:09 NEW PHASE (Andre, go): cross-site class pools. Target: TD pools = capability classes (fed-gpu/fed-cpu) with MEMBERS (endpoint+queue+account+sizes+attributes such as site/software); pilots carry member attributes; per-task requirements forwarded to rhapsody; attribute-aware pick_dispatch + scale-up; budget/accounting per member; federation join = add member(s) to class pools, pick = class; CLI/UI/demo follow. Demo date may move. Plans: orbit plans/120 (requirements passthrough, in progress), plans/121 (multi-member pools), atomic plans/08 (federation onto class pools).
- 2026-09-07 12:16 plan 120 (requirements passthrough) written: rhapsody concurrent/dragon_v3 do NOT enforce resources -> v1 must reserve cores/GPUs in the dispatcher (derived from RUNNING tasks), validate per-node vs pilot size (400), per-backend kwargs mapping; v2 = in-pilot pinning. Waiting for 121/08.
- 2026-09-07 12:19 DECISION (Andre): no dispatcher-side core/GPU reservation this round; forward requirements to rhapsody only. Plan 120 annotated; TODO recorded in project.orbit.
- 2026-09-07 12:27 Andre: plan 120 -> fable plan review (iterate) -> opus impl -> fable review (iterate) -> PR. Worktree ~/radical/radical.orbit-treq on branch feature/task-requirements (off devel) for the dispatcher part; federation-side forwarding lands on feature/atomic-federation afterwards.
- 2026-09-07 12:32 plans 121 (multi-member class pools, orbit) + 08 (federation/CLI/UI/demo onto class pools) written; fable reviews running for 120 and for 121+08.
- 2026-09-07 12:37 plan 120 review r1: 3 BLOCKING (deferred sections still in body, fed-only deps untagged, endpoint-mode mapping undecided -> advisory only) + 11 SHOULD; author agent resumed to revise, incl. 121 amendments (forward software/labels). 121+08 review r1 running.
- 2026-09-07 12:41 plans 121+08 review r1: 121 6 BLOCKING (legacy self-promotion on persist, implicit member construction, scratch mkdir on broker, _pool_dir replay, per-member node-hours, input staging to non-shared members) + 08 3 BLOCKING. Decisions: D1 inputs travel with the submit (inputs_b64; dispatcher places before rhapsody submit); D2 drop suspect-quiesce; D3 local_c gets a gpu member (fed-gpu spans 2 sites); D4 cut creep (no LIVE rename here, no compat wrappers). Planner resumed.
- 2026-09-07 12:46 plan 120 revised (517 lines, treq numbering, Deferred section, endpoint mode advisory-only, software/labels forwarded); review r2 running. 121/08 revision in progress.
- 2026-09-07 12:54 plan 120 review r2: no BLOCKING, 10 SHOULD (dialect merge site, backend_kwargs({})=={}, exact 400 strings, bool rejection, endpoint-mode details, mixed-backend gate, labels type str|int|float, fed neutral set incl labels). Opus implementer launched on feature/task-requirements (worktree) with these as amendments.
- 2026-09-07 12:56 plans 121 (1115 l) + 08 (891 l) revised; §3/§4 frozen. r2 review launched; 08 atomic-side implementer (CLI/UI/runner/demo) launched against the frozen contract. 120 implementation running.
- 2026-09-07 13:15 plans 121+08 review r2: 121 3 BLOCKING (PilotRecord.endpoint_name snapshot, TaskRecord.spooled separate from inputs, emptied class pool must replay) + 08 1 BLOCKING (upgrade: register legacy sid then unregister) + labels type cross-plan. Decisions D5 (GPU members cpus=1 + train 10 s -> deterministic 2-member spread), D6 (mpi gate: reject iff every member dragon_v1), D7 (multi_member default = members present; explicit false wins; declaration body spelled out), D8 (DELETE fail_unsatisfiable flag), D9 (labels str|int|float). Planner resumed; 120 + 08a implementers informed.
- 2026-09-07 13:25 plans 121 (1280 l) + 08 (996 l) FINAL after r2; frozen §3/§4/§9. 121 impl waits for 120 code in the worktree. 120 impl + 08 atomic-side impl running.
- 2026-09-07 13:26 120 IMPLEMENTED + committed on feature/task-requirements (1113 tests). Review launched. Worktree ~/radical/radical.orbit-cp = feature/class-pools (stacked on 120) for the 121 implementation, launched.
- 2026-09-07 13:36 120 code review: no BLOCKING; 5 SHOULD (makeflow CORES/GPUS capture docs, cores defaults to max(1,ranks) when absent, stale comment, requirements key in responses documented, 4 extra tests) -> implementer resumed; PR after fixes. 121 impl + 08a impl running.
- 2026-09-07 13:49 API timeout killed all three implementers mid-flight (120 fixes, 121, 08a); all three resumed with context intact (partial work on disk in their trees).
- 2026-09-07 13:54 120 review fixes committed (1130 tests). Opening PR feature/task-requirements -> devel via /pr.
- 2026-09-07 13:57 PR #123 opened: feature/task-requirements -> devel (https://github.com/radical-cybertools/radical.orbit/pull/123). Watch armed for CI + review comments.
- 2026-09-07 14:00 PR #123 CI GREEN (lint + py3.10/3.11/3.12 + GitGuardian); 0 reviews/comments so far; watch re-armed for review activity.
- 2026-09-07 14:00 08 atomic-side IMPLEMENTED + committed (343 tests; expects federation routes per 08 §4 with members[]/class/tasks_failed/inputs_b64; never sends cwd). Review launched. 121 impl running; PR #123 CI green, watch armed.
- 2026-09-07 14:06 08a review: 1 BLOCKING (class-pool pilot-kill regex matches sibling resources with shared suffix -> persist joined record at join, exact patterns at leave) + 6 SHOULD (round-trip-only attribute coercion, train duration 30 s + honest determinism bound, reason names resource only after placement, reject --software/--declare with --member, 400->NO_RESOURCE only for the two dispatcher phrases, tolerant client reads) -> implementer resumed. Notes for the federation implementer: fill member scratch_base from the record; per-member usage must carry tasks_running/tasks_done; task poll may include class.
- 2026-09-07 15:55 resumed after suspend: 121 impl (2h) + 08a fixes (1h) still running; PR #123 no review activity in 1h (watch timed out normally); watch re-armed 2h.
- 2026-09-07 15:56 08a review fixes committed (377 tests). Remaining: 121 impl (running), then merge 120 fix commit into class-pools, review, then federation rework (08 orbit part) on feature/atomic-federation.
- 2026-09-07 15:57 federation rework (08 orbit part) launched on feature/atomic-federation against the frozen 121 contract with a fake dispatcher API; co-hosted integration tests deferred until feature/class-pools is merged in. 121 impl still running.
- 2026-09-07 16:21 121 IMPLEMENTED + committed (f2d2874, 1294 tests); 120 fixes merged into class-pools (2fa338e, clean). 121 review launched.
- 2026-09-07 16:31 121 review: 1 BLOCKING outside the diff (plugin_staging allow-list rejects puts to real scratch paths -> extend with $RADICAL_ORBIT_SCRATCH_BASE) + 8 SHOULD (rh-dialect cwd marker, node_hours from active_at only, floor guard fall-through, legacy min_pilots change documented, mkdir before shared copy, expanduser only when shared, legacy member_id empty not _, cancel_tasks race) -> implementer resumed. Merge-collision list for feature/atomic-federation recorded by the reviewer (finished_at, pilot_history, on_tick, node_hours re-export, test/doc tails).
- 2026-09-07 16:45 federation rework (08 orbit) IMPLEMENTED + committed on feature/atomic-federation (1352 tests; 10 co-hosted tests skipped until merge). Waiting for 121 fixes, then merge class-pools -> atomic-federation.
- 2026-09-07 16:46 federation rework committed (ac3818b); pre-merge fable review launched. 121 fixes running. Next: merge class-pools -> atomic-federation, un-skip co-hosted tests, local demo run, PRs.
- 2026-09-07 16:54 federation pre-merge review: 1 BLOCKING (pre-08 _derive_member emits None attributes -> 121 parser rejects -> all joins blocked after upgrade) + 7 SHOULD (cancel_tasks orphaning QUEUED ledger entries, stale placement not cleared, upgrade-failed poll 404, member-id length, satisfies verbatim, negative remaining, extra merge hunks: orphan-pool guard + task_status_batch/_on_task_status + uid pre-registration must survive the merge) -> implementer resumed. MERGE CHECKLIST: keep fed-branch hunks (orphan guard, batch events, uid pre-reg), take 121 on_tick wholesale, dedupe finished_at stamp, take 121 pilot_history, federation_state.node_hours_from_history -> re-export task_dispatcher_state.node_hours (active_at-only semantics), swap federation_policy.satisfies import to task_dispatcher_match, plans/120+121 add/add: keep -cp versions.
- 2026-09-07 17:08 Claude Code process restarted; the 121-fix agent, federation-fix agent and PR watch were lost. 121 fixes partially on disk in -cp (15 files, uncommitted); federation fixes not started. Relaunching fresh agents + watch.
- 2026-09-07 17:13 121 review fixes committed on feature/class-pools (1330 tests). Federation fixes running; merge next.
- 2026-09-07 17:42 MERGE feature/class-pools -> feature/atomic-federation started (--no-commit): conflicts in plugin_task_dispatcher.py, strategy_conservative.py, test_plugin_task_dispatcher.py, rest_api.md (plans resolved with -cp versions). Resolution + post-merge TODOs + co-hosted un-skip delegated.
- 2026-09-07 18:05 MERGED class-pools into feature/atomic-federation (42ea1bd): 1688 tests, 0 co-hosted skips, post-merge TODOs done. Next: local demo run with class pools.
- 2026-09-07 18:08 CLASS-POOL DEMO PASSES on localhost (first run after the merge): 3 resources / 5 members; md -> local_a.default (fed-cpu); train -> local_b.gpu + local_c.gpu (fed-gpu, 2 sites); acc 0.966>0.907>0.828; 78 s; teardown clean. Opening stacked PRs (class-pools -> task-requirements; atomic-federation -> class-pools).
- 2026-09-07 18:09 PRs opened: #124 feature/class-pools -> feature/task-requirements; #125 feature/atomic-federation -> feature/class-pools (stacked on #123). Watches armed. Merge-resolution review running.
- 2026-09-07 18:15 merge-resolution review: PR-ready; 2 cleanups committed + pushed (#125 updated). Tuesday prerequisites recorded in demo/local/README: remote members need shared_fs=false + explicit scratch_base; use allocation mode (psij executor detected on the broker host). PR watches armed (#123 #124 #125).
- 2026-09-07 21:14 PR watches: no review activity on #123/#124/#125 all evening; letting the watches expire overnight instead of re-arming (re-arm on the next "continue").
- 2026-09-08 00:35 demo/local split into broker.sh / join.sh / submit.sh (+ up.sh orchestrator); automated + manual paths verified.

## 2026-09-08 — demo harness: self-installing pinned stack, resource selector, Odo

- `demo/2026_09_08/env.sh` pins `radical.orbit@feature/atomic-federation` and
  `ATOMIC@feature/demo-wm`; `ensure_stack` uses a clean pinned checkout if
  present, otherwise clones under `/tmp/atomic-demo/src`, `git pull --ff-only`
  per run, reinstalls only when the commit changed (stamp in the venv) or
  `--reinstall`. Trigger: broker on host three died with "No plugin matches
  'federation'" because it installed from a checkout on the wrong branch.
- Single resource selector (`local|local_a|local_b|local_c|r3|perlmutter|odo`)
  passed through all role scripts; `$SLURM_JOB_ID` picks allocation vs login
  mode; Bridges replaced by Odo; `TODO(...)` placeholders for real
  queue/account/scratch (join refuses until filled).
- Fixed: pilot-wait list was hard-pinned to `local_a` for every resource.
- README opens with the presentation-facing overview (slides link, steps↔slides
  table, real vs simulated).
- Verified: fast path cycle DONE in 78 s; forced fresh-venv clone 94 s, rerun
  7 s "up to date"; wrong-branch checkout refused; 57 helper tests pass.
- 2026-09-08 16:40 host three: broker.sh installed the pinned stack fine (clone on wrong-branch checkout) but the broker died: it bound to the literal 'r3', which is a laptop ssh alias and does not resolve there. Fix: ATOMIC_DEMO_BROKER_BIND (0.0.0.0 on r3) split from ATOMIC_DEMO_BROKER_HOST (r3: own FQDN/IP; perlmutter/odo: must be exported, join/submit refuse otherwise); broker.sh prints the export + scp lines for the remote hosts. Verified: r3 path (broker + self-join) and full local cycle on a wildcard bind, DONE; 57 tests.
- 2026-09-08 17:20 Orbit explorer dispatcher tab rendered the `fed` session group as a bogus legacy pool card (GET /pools is grouped by session). Fixed on feature/atomic-federation (75db22d): per-session cards, verbose class pools. Perlmutter: python3 is 3.6 on compute nodes -> env.sh loads python/3.12-26.1.0 (2ad9d5d); home quota exceeded on venv creation -> venv+clones under $SCRATCH/demo (ATOMIC_DEMO_HOME; odo: $MEMBERWORK/demo).
